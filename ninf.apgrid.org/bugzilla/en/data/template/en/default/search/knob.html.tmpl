#------------------------------------------------------------------------
# Compiled template generated by the Template Toolkit version 2.09
#------------------------------------------------------------------------

Template::Document->new({
    METADATA => {
        'modtime' => '1024790160',
        'name' => 'search/knob.html.tmpl',
    },
    BLOCK => sub {
        my $context = shift || die "template sub called without context\n";
        my $stash   = $context->stash;
        my $output  = '';
        my $error;
        
        eval { BLOCK: {
    $output .=  "<!-- 1.0\@bugzilla.org -->\n";
    $output .=  "\n\n";
    $stash->set('ordersdesc', { 'Reuse same sort as last time' => 'Reuse same sort as last time', 'Bug Number' => 'Bug Number', 'Importance' => 'Importance', 'Assignee' => 'Assignee', 'Last Changed' => 'Last Changed'  });
    $output .=  "\n\n<br>";
    if (! $stash->get('userid')) {
    $output .=  "\n   <input type=\"hidden\" name=\"cmdtype\" value=\"doit\">";
    }
    else {
    $output .=  "\n   <input type=\"radio\" name=\"cmdtype\" value=\"doit\" checked=\"checked\">\n     Run this query\n   <br>";
    if ($stash->get(['namedqueries', 0, 'size', 0]) > 0) {
    $output .=  "\n     <br>\n     <table cellspacing=\"0\" cellpadding=\"0\">\n       <tr>\n         <td>\n           <input type=\"radio\" name=\"cmdtype\" value=\"editnamed\">\n           Load my remembered query:\n         </td>\n         <td rowspan=\"3\">\n           &nbsp;&nbsp;<select name=\"namedcmd\">";
    
    # FOREACH 
    do {
        my ($value, $error, $oldloop);
        my $list = $stash->get('namedqueries');
        
        unless (UNIVERSAL::isa($list, 'Template::Iterator')) {
            $list = Template::Config->iterator($list)
                || die $Template::Config::ERROR, "\n"; 
        }
    
        ($value, $error) = $list->get_first();
        eval { $oldloop = $stash->get('loop') };
        $stash->set('loop', $list);
        eval {
    LOOP:   while (! $error) {
                $stash->{'query'} = $value;
    $output .=  "\n               <option value=\"";
    
    # FILTER
    $output .=  do {
        my $output = '';
        my $filter = $context->filter('html')
                  || $context->throw($context->error);
    
    $output .=  $stash->get('query');
        
        &$filter($output);
    };
    
    $output .=  "\">";
    
    # FILTER
    $output .=  do {
        my $output = '';
        my $filter = $context->filter('html')
                  || $context->throw($context->error);
    
    $output .=  $stash->get('query');
        
        &$filter($output);
    };
    
    $output .=  "</option>";;
                ($value, $error) = $list->get_next();
            }
        };
        $stash->set('loop', $oldloop);
        die $@ if $@;
        $error = 0 if $error && $error eq Template::Constants::STATUS_DONE;
        die $error if $error;
    };
    
    $output .=  "\n           </select>\n         </td>\n       </tr>  \n       <tr>\n         <td>\n           <input type=\"radio\" name=\"cmdtype\" value=\"runnamed\">\n           Run my remembered query:\n         </td>\n       </tr>\n       <tr>\n          <td>\n           <input type=\"radio\" name=\"cmdtype\" value=\"forgetnamed\">\n           Forget my remembered query:\n         </td>\n       </tr>\n     </table>\n     <br>";
    }
    
    $output .=  "\n   <input type=\"radio\" name=\"cmdtype\" value=\"asdefault\">\n     Remember this as my default query\n   <br>\n   <input type=\"radio\" name=\"cmdtype\" value=\"asnamed\">\n     Remember this query, and name it: \n     <input type=\"text\" name=\"newqueryname\">\n     <br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\n     <input type=\"checkbox\" name=\"tofooter\" value=\"1\">\n       and put it in my page footer";
    }
    
    $output .=  "\n\n  <p>\n    Sort results by:\n    <select name=\"order\">";
    
    # FOREACH 
    do {
        my ($value, $error, $oldloop);
        my $list = $stash->get('orders');
        
        unless (UNIVERSAL::isa($list, 'Template::Iterator')) {
            $list = Template::Config->iterator($list)
                || die $Template::Config::ERROR, "\n"; 
        }
    
        ($value, $error) = $list->get_first();
        eval { $oldloop = $stash->get('loop') };
        $stash->set('loop', $list);
        eval {
    LOOP:   while (! $error) {
                $stash->{'order'} = $value;
    $output .=  "\n      <option value=\"";
    
    # FILTER
    $output .=  do {
        my $output = '';
        my $filter = $context->filter('html')
                  || $context->throw($context->error);
    
    $output .=  $stash->get('order');
        
        &$filter($output);
    };
    
    $output .=  "\"";
    if ($stash->get(['default', 0, 'order', 0, 0, 0]) eq $stash->get('order')) {
    $output .=  ' selected';
    }
    
    $output .=  ">";
    
    # FILTER
    $output .=  do {
        my $output = '';
        my $filter = $context->filter('html')
                  || $context->throw($context->error);
    
    $output .=  $stash->get(['ordersdesc', 0, $stash->get('order'), 0]);
        
        &$filter($output);
    };
    
    $output .=  "</option> ";;
                ($value, $error) = $list->get_next();
            }
        };
        $stash->set('loop', $oldloop);
        die $@ if $@;
        $error = 0 if $error && $error eq Template::Constants::STATUS_DONE;
        die $error if $error;
    };
    
    $output .=  "\n    </select>\n\n    <input type=\"submit\" value=\"";
    $output .=  $stash->get('button_name');
    $output .=  "\">";
    if ($stash->get('userdefaultquery')) {
    $output .=  "\n       <p>\n         <a href=\"query.cgi?nukedefaultquery=1\">\n           Set my default query back to the system default</a>\n       </p>";
    }
    
    $output .=  "\n  </p>\n";
        } };
        if ($@) {
            $error = $context->catch($@, \$output);
            die $error unless $error->type eq 'return';
        }
    
        return $output;
    },
    DEFBLOCKS => {

    },
});
